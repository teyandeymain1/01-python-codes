#---------------------------------- 行列を表示する---------------------------------------
def print_matrix(inMatrix):
    for rowList in inMatrix:         
        print("".join(map(str, rowList)))
#=====================================================